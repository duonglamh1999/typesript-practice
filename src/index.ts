// create tsconfig
//window: tsc --init
//mac: touch tsconfig.json 

let age:number  = 20;

//declare variable
const add = (x:number,y:number): number =>{
    return x +y
}

//arrray with type
let numbers: number[] = [];

//tuple 
let user: [number,string] = [1,'cute'];

//
